<style>
@import url('https://fonts.googleapis.com/css2?family=Merienda:wght@500&family=Oswald&family=Roboto:wght@500&display=swap');
    nav{
        z-index: 1;
        font-family: 'Roboto', sans-serif;
        font-size: 20px;
        color: #FFFFFF;
    }

.log3 {
    color: white;

}

.log4:hover {
    color: #FF9000;
}



</style>



<nav class="navbar navbar-expand-lg ">
    <div class="container-fluid">

        <a class="navbar-brand" href="{{path('main')}}">
            <img class"" style="w-30" src="{{asset('/assets/uploads/LOGO1.png')}}">
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link log4 log3" aria-current="page" href="{{path('main')}}">Accueil</a>
                </li>

                <li class="nav-item">
                    <a class="nav-link log4  log3" href="{{ path('products_index')}}">Nos produits</a>
                </li>
                {# <li class="nav-item">
                    <a class="nav-link" href="#">Homme</a>
                </li> #}
                <li class="nav-item">
                    {# <a class="nav-link log4 log3" href="{{path('app_qui')}}">Qui sommes nous ?</a> #}
                </li>
                <li class="nav-item">
                    <a class="nav-link log4 log3" href="{{path('cart_index')}}">Panier</a>
                </li>
                {# --- pout plus tard --- #}

                {# <li class="nav-item">
                    <a class="nav-link" href="#">Nous contacter</a>
                </li> #}

            </ul>

            <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
                {% if app.user %}
                    {% if is_granted('ROLE_PRODUCT_ADMIN') %}
                        <li class='nav-item'>
                            {# <a class="nav-link" href="{{ path('admin_index')}}">Administration</a> #}
                            <a class="nav-link log4  log3" href="{{ path('admin_index')}}">Administration</a>
                        </li>
                    {% endif %}
                    <li class="nav-item">
                        {# <a class="nav-link" href="#">Mon compte</a> #}
                        <a class="nav-link log4  log3" href="{{path('profil_index')}}">Mon compte</a>
                    </li>

                    <li class="nav-item">
                        {# <a class="nav-link" href="{{path('payment_orders')}}">Mes commandes</a> #}
                        {# <a class="nav-link log4  log3" href="{{path('payment_orders')}} ">Mes commandes</a> #}
                    </li>

                    <li class="nav-item">
                        {# <a class="nav-link" href="{{path('app_logout')}}">Me déconnecter</a>  #}
                        <a class="nav-link log4  log3" href="{{path('app_logout')}} ">Me déconnecter</a>
                    </li>

                {% else %}
                    {# <li class="nav-item"> #}
                        <li class="nav-item">
                            {# <a class="nav-link" href="{{path('app_login')}}">Me connecter</a>  #}
                            <a class="nav-link log3" href="{{path('app_login')}} ">Me connecter</a>
                        </li>

                        <li class="nav-item">
                            {# <a class="nav-link" href="{{path('app_register')}}">M'inscrire</a>  #}
                            <a class="nav-link log3" href="{{path('app_register')}} ">M'inscrire</a>
                        </li>
                    {# </li> #}
                {% endif %}
            </ul>
        </div>
    </div>
</nav>

